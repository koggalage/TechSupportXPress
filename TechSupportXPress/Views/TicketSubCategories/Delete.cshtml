@model TechSupportXPress.Models.TicketSubCategory

@{
    ViewData["Title"] = "Delete";
}

<div class="mb-3">
    <a asp-action="Index" asp-route-id="@Model.CategoryId" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Back to Sub Categories
    </a>
</div>

<div class="card rounded-4">
    <div class="card-body">
        <div class="d-flex align-items-center">
            <h4 class="mb-3 text-danger"><i class="bi bi-trash-fill"></i> Delete Ticket Sub Category</h4>
        </div>
        <hr />
        <div class="row">
            <table class="table table-bordered table-responsive table-hover table-responsive-lg">
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Category)</th>
                    <td>@Html.DisplayFor(model => model.Category.Name)</td>
                    <th>@Html.DisplayNameFor(model => model.Code)</th>
                    <td>@Html.DisplayFor(model => model.Code)</td>
                </tr>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <td colspan="3">@Html.DisplayFor(model => model.Name)</td>
                </tr>
            </table>
        </div>
    </div>
</div>

<div class="row p-3">
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger float-end">
            <i class="fadeIn animated bx bx-save"></i> Delete
        </button>
    </form>
</div>
